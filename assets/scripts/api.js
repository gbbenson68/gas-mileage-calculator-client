'use strict'
const pkgName = 'api'

/*
** signUp()
*/
const signUp = () => {
  const whoAmI = `${pkgName}.signUp()`
}

/*
** signIn()
*/
const signIn = () => {
  const whoAmI = `${pkgName}.signIn()`
}

/*
** signOut()
*/
const signOut = () => {
  const whoAmI = `${pkgName}.signOut()`
}

/*
** changePassword()
*/
const changePassword = () => {
  const whoAmI = `${pkgName}.changePassword()`
}

/*
** createReading()
*/
const createReading = () => {
  const whoAmI = `${pkgName}.createReading()`
}

/*
** updateReading()
*/
const updateReading = () => {
  const whoAmI = `${pkgName}.updateReading()`
}

/*
** index()
*/
const index = () => {
  const whoAmI = `${pkgName}.index()`
}

/*
** show()
*/
const show = () => {
  const whoAmI = `${pkgName}.show()`
}

module.exports = {
  signUp,
  signIn,
  signOut,
  changePassword,
  createReading,
  updateReading,
  index,
  show
}
